// TEMP:vlm-flag — Delete this entire file when VLM features are stable
/**
 * Feature flags for controlling experimental features.
 * Set to true to enable the feature.
 */
export const featureFlags = {
  /** Enable VLM pipeline selection, VLM model picker, and hanja auto-fallback */
  enableVlm: false,
};
